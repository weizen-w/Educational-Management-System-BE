spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:/test_EMS_db
  jpa:
#    hibernate:
#      ddl-auto: update
    hibernate:
      ddl-auto: create-drop
  liquibase:
    contexts: test

s3:
  accessKey: fake
  endpoint: fake
  region: fake
  secretKey: fake

base:
  url: test